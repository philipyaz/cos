# Cos — machine-local secrets.  TEMPLATE.
#
# Copy this to `config/secrets.env` (gitignored) and fill in the real value:
#     cp config/secrets.env.example config/secrets.env
#     $EDITOR config/secrets.env
#
# This is the ONE secret the MCP bridges need. The vault MCP bridge (:8005) is the only
# bridge that makes OUTBOUND LLM calls — it embeds the Claude Agent SDK and runs a headless
# session per tool call — so it needs a real Anthropic API key. The other four bridges
# (board/openwhispr/calendar/guard) talk only to localhost and carry no secret.
#
# The vault bridge's launch wrapper (mcp/vault-server/launch.sh, invoked by the launchd
# plist) sources THIS file at startup and exports the key into the server's environment, so
# the secret lives in exactly one machine-local file — never in the installed plist, never
# committed.  Rotate by editing this file and restarting the bridge:
#     launchctl kickstart -k gui/$(id -u)/com.chiefofstaff.mcp-vault
#
# Get a key at https://console.anthropic.com → Settings → API keys → Create Key.
# NOTE: this is billed API usage (separate from any Claude subscription) — each vault tool
# call is a full model session and costs tokens.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- Health & Athlete add-on push token -------------------------------------------------------
# Shared secret for the Health add-on's ingest endpoint (POST /api/health/push). The iPhone
# Shortcut / Health Auto Export client sends it as the `x-health-token` header, and the health
# MCP's write tools (push/delete) attach it automatically. Reads are ungated; only writes need it.
# Pick any random string (e.g. `openssl rand -hex 20`) — it's machine-local and never committed.
HEALTH_PUSH_TOKEN=replace-with-a-random-token
