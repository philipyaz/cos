{
  "schemaVersion": 1,
  "name": "calendar",
  "kind": "bridge",
  "runtime": "bridge",
  "core": true,
  "roles": [
    "hub",
    "spoke"
  ],
  "portVar": "CALENDAR_BRIDGE_PORT",
  "stdio": [
    "${NODE_BIN}",
    "${REPO_ROOT}/mcp/calendar-server/server.mjs"
  ],
  "env": {
    "CRM_BASE_URL": "${BOARD_URL}",
    "COS_DEVICE_ID": "${COS_DEVICE_ID}",
    "COS_DEVICE_ROLE": "${COS_DEVICE_ROLE}"
  },
  "idleExit": true,
  "clients": [
    "claude-code",
    "cowork"
  ],
  "probe": {
    "type": "httpListen"
  }
}
